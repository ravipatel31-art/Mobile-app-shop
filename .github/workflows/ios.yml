name: Build iOS IPA

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-15
    defaults:
      run:
        working-directory: mobile
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build iOS
        env:
          API_BASE_URL: ${{ vars.API_BASE_URL }}
        run: |
          if [ -z "$API_BASE_URL" ]; then
            echo "::error::Repository variable API_BASE_URL is not set"
            exit 1
          fi
          flutter clean
          flutter pub get
          # Patch Xcode project to remove Development Team requirement
          sed -i '' 's/DEVELOPMENT_TEAM = "[^"]*"/DEVELOPMENT_TEAM = ""/g' ios/Runner.xcodeproj/project.pbxproj
          sed -i '' 's/CODE_SIGN_STYLE = Automatic/CODE_SIGN_STYLE = Manual/g' ios/Runner.xcodeproj/project.pbxproj
          sed -i '' 's/ProvisioningStyle = Automatic/ProvisioningStyle = Manual/g' ios/Runner.xcodeproj/project.pbxproj
          # Re-run flutter to regenerate project files (keeps our signing changes)
          flutter pub get
          flutter build ios --release --no-codesign \
            --dart-define=API_BASE_URL="$API_BASE_URL"

      - name: Package IPA
        run: |
          cd mobile
          # Find Runner.app in any build output path
          for dir in \
            build/ios/iphoneos/Runner.app \
            build/ios/Release-iphoneos/Runner.app \
            build/ios/Debug-iphoneos/Runner.app; do
            if [ -d "$dir" ]; then
              APP_PATH="$dir"
              break
            fi
          done
          if [ -z "$APP_PATH" ]; then
            echo "::error::Runner.app not found"
            find build/ios -name "Runner.app" -type d 2>/dev/null
            exit 1
          fi
          echo "Found Runner.app at: $APP_PATH"
          # Verify it's a real app (not just plugin bundles)
          if [ ! -f "$APP_PATH/Runner" ]; then
            echo "::error::Runner.app is incomplete - missing Runner binary"
            ls -la "$APP_PATH/"
            exit 1
          fi
          ls -lh "$APP_PATH/Runner"
          mkdir Payload
          cp -r "$APP_PATH" Payload/
          zip -r cafe-app-unsigned.ipa Payload
          rm -rf Payload
          ls -lh cafe-app-unsigned.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: cafe-ios-ipa
          path: mobile/cafe-app-unsigned.ipa
          retention-days: 30
