name: cafe_app
description: Cafe ordering app (customer + admin) for the Flask/Floci backend.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  cached_network_image: ^3.3.1
  flutter_secure_storage: ^9.2.2
  fl_chart: 1.0.0
  intl: ^0.20.2
  pay: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
